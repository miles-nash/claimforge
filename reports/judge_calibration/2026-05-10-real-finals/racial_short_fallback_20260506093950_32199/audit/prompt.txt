You are a strict claim-level scientific replication judge.

Use only the supplied research question, ground-truth conclusion, and agent conclusion. Do not use outside knowledge. Break each conclusion into atomic scientific/task-relevant claims, then score. Do not include artifact inventories, file paths, links, packaging status, validation status, or generic run-status bookkeeping as agent_claims or ground_truth_claims unless they directly state scientific evidence, access blockers, data counts, experimental design, measured metrics, or blocked/unblocked task conclusions.

- Precision: fraction of agent claims supported by the ground truth.
- Recall: fraction of ground-truth claims covered by the agent.
- F1: harmonic mean of precision and recall.
- unsupported_overclaims: scientific/task-result claims that assert results, measurements, or conclusions contradicted by the ground truth or absent from it. Do not list run-status, artifact, validation, or packaging claims such as "completed the blocked-run package", "wrote artifacts", "validation passed", "no final message", or "return code 124" as overclaims; ignore those bookkeeping claims for this claim-quality score.

Return only valid JSON matching this shape:

{
  "agent_claims": [
    "atomic claim from the agent conclusion"
  ],
  "ground_truth_claims": [
    "atomic claim from the supplied ground truth"
  ],
  "precision_checks": [
    {
      "agent_claim": "claim",
      "supported_by_ground_truth": true,
      "rationale": "short reason"
    }
  ],
  "recall_checks": [
    {
      "ground_truth_claim": "claim",
      "covered_by_agent": true,
      "rationale": "short reason"
    }
  ],
  "unsupported_overclaims": [
    "scientific or task-result claim that asserts an unmeasured or contradicted result"
  ],
  "precision": 1.0,
  "recall": 1.0,
  "f1": 1.0
}

Research question:
Does GPT-3.5 predict higher medical costs or longer hospital stays disproportionately for certain racial groups on paired medical vignettes?

Ground-truth conclusion:
The run is access-blocked, not a test of the original GPT-3.5 racial-bias claim. Local data contained 100 patient records. A paired counterfactual audit design was built in which every patient appears under each race/ethnicity condition. The dry-run manifest planned 600 requests for gpt-3.5-turbo and 600 for gpt-4, within the 1,000-call/model budget. Explicit identity/nationality cues were redacted in prompt construction. No external model calls were executed because OPENAI_API_KEY and required Python imports including openai were missing. It is valid to claim that the audit design is locally ready and valid. It must not claim that GPT-3.5 or GPT-4 race sensitivity, cost differences, length-of-stay differences, or model bias was measured.


Agent conclusion:
Completed the run as a blocked-model fallback.

Key result: the original GPT-3.5 claim was **not tested** because this environment is missing `OPENAI_API_KEY` and required Python imports including `openai`. I did not attempt external model calls.

Artifacts written:
- `access_check.json`
- `data_audit.json`
- `prompt_manifest.jsonl`
- `prompt_preview.json`
- `scoring_manifest.jsonl`
- `local_baselines.json`
- `run_summary.json`

What was measured locally:
- Dataset loaded: 100 patient records.
- Built a fair paired counterfactual audit: every patient appears under each race/ethnicity condition.
- Generated 600 planned requests for `gpt-3.5-turbo` and 600 for `gpt-4`, within the 1000-call/model budget.
- Redacted explicit identity/nationality cues in prompt construction to avoid leakage.

Conclusion: measured evidence only supports that the audit design is ready and locally valid. The claim "GPT-3.5 predicts higher medical costs or longer hospital stays for certain racial groups" remains **blocked, not supported or refuted**. The next unblocker is to provide `OPENAI_API_KEY` and install the missing SDK dependencies.

