# 2026-05-06 Log

## Cycle 0: Setup And Target Selection

Created the 30-minute heartbeat automation: `frontier-push-loop`.

Initial research points toward autonomous scientific verification rather than autonomous paper generation.

Evidence:

- AI Scientist and AI Scientist-v2 show end-to-end research automation is becoming real, but this increases the value of independent verification.
- CORE-Bench frames computational reproducibility as a real-world benchmark, with the best reported agent only reaching 21% accuracy on the hardest task in the abstract.
- PaperBench evaluates full paper replication and reports a 21.0% average replication score for the best tested agent in OpenAI's April 2025 release.
- FIRE-Bench reports low current performance on full-cycle research rediscovery and identifies planning as a major failure source.
- Reproducible LM evaluation work emphasizes transparency and setup sensitivity as core evaluation problems.

Decision:

Target the narrow field of Codex-native research verification: planning and execution workflows for reproducing/falsifying computational claims.

Next:

1. Locate and inspect the public benchmark repos for CORE-Bench, PaperBench, and FIRE-Bench.
2. Choose the smallest runnable task family.
3. Build a local evidence ledger template.
4. Request missing access from Miles only when a concrete run is blocked.

Update:

Miles clarified that work should run through Codex tokens rather than OpenAI API billing. Revised access requirements accordingly: `OPENAI_API_KEY` is optional, while `HF_TOKEN`, GitHub access, and a reliable Codex CLI/subscription path are the practical unblockers.

## Cycle 1: Harness Work

Built a local FIRE-Bench runner that uses `npx @openai/codex@latest`, because the upstream pinned `@openai/codex@0.39.0` cannot use the current ChatGPT/Codex account with `gpt-5` or `gpt-5.5`.

Validation:

- `@openai/codex@latest` smoke test succeeded with `gpt-5.5`.
- FIRE-Bench inventory script cloned and listed verified tasks.
- Runner dry-run created the expected log/work directory layout.

Started first real 10-minute rollout: `claimforge-codex` on `awareness_detection`.

Result:

- Run completed successfully in 456.85 seconds.
- It produced an evidence ledger, experiment script, model-access test, baseline metrics, and predictions.
- Direct frontier-model calls were blocked by missing `OPENAI_API_KEY`, `GOOGLE_API_KEY`, and `ANTHROPIC_API_KEY`.
- Fallback artifact baseline was strong: transcript CV logistic AUC 0.948 overall, 0.863 on chat transcripts, 0.992 on agentic transcripts.

Interpretation:

This suggests the `awareness_detection` task may be strongly solvable from source-distribution artifacts, so model success on this task should be compared against simple transcript baselines before being interpreted as "evaluation awareness."

Harness issue found:

A raw baseline run discovered hidden `conclusion.txt` by walking the local FIRE-Bench checkout. I killed that run and marked it invalid. Patched the runner so live work/log directories sit outside the FIRE-Bench checkout and added a benchmark-hygiene instruction forbidding evaluator-only files. Existing ClaimForge rollout did not visibly access hidden conclusions, but should be treated as provisional until rerun with the patched runner.

Clean rerun:

- `claimforge-codex` on `awareness_detection`, run id `20260505221550_13481`, completed in 527.82 seconds.
- Stayed focused on copied task assets.
- Produced `evidence_ledger.md`, `experiment_plan.md`, runnable scripts, human-label metrics, and surface-cue baseline.
- Frontier model experiment remained blocked by absent model API keys.
- Human mean AUC: 0.919 overall, 0.996 agentic, 0.819 chat.
- Surface-cue logistic AUC: 0.751 overall, 0.814 agentic, 0.626 chat.

Task triage:

Added `scripts/firebench_task_triage.py`. It reads only instructions and local data metadata, avoiding evaluator-only files. Initial result: every verified FIRE-Bench task appears model/API-oriented. Good Codex-only targets are therefore tasks with local datasets where useful fallback baselines are possible, not tasks that can fully satisfy the original frontier-model claim without API access.

Clean raw baseline:

- Ran `codex-raw` on `awareness_detection` with patched isolated runner, run id `20260505222735_15324`.
- It obeyed the hygiene boundary and saw only `.`, `data`, and `utils`.
- It timed out after 600.01 seconds with return code 124.
- It produced no final message, no experiment script, no metrics, and no report.

Comparison:

ClaimForge completed in 527.82 seconds with artifacts; raw Codex timed out at 600 seconds after an initial plan. This is evidence that the ClaimForge protocol improves execution under access-blocked research conditions.

Token strategy update:

Added `docs/token_strategy.md` and `state/next_actions.md`. Future automation fires should pick exactly one cycle mode, produce one durable artifact, update state, and commit/push coherent progress. Current best token use is a second clean raw-vs-ClaimForge pair on `llm_racial_bias_in_medicine` with 300-second caps.
